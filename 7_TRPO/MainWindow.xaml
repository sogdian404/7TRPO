<Window x:Class="_7_TRPO.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_7_TRPO"
        mc:Ignorable="d"
        Title="Больничка" Height="700" Width="900">
    <!--<Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="350"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <WrapPanel>
            <Border BorderBrush="Black" BorderThickness="1" Margin="5" Height="245" Width="180">
                <StackPanel Margin="5" x:Name="D_RegisterForm">
                    <TextBlock Text="Фамилия:"/>
                    <TextBox Text="{Binding LastName}"/>
                    <TextBlock Text="Имя:"/>
                    <TextBox  Text="{Binding Name}"/>
                    <TextBlock Text="Отчество:"/>
                    <TextBox  Text="{Binding MiddleName}"/>
                    <TextBlock Text="Специальность:"/>
                    <TextBox  Text="{Binding Specialisation}"/>
                    <TextBlock Text="Пароль:"/>
                    <TextBox  Text="{Binding Password}" x:Name="Pass1"/>
                    <TextBlock Text="Подтвердите пароль:"/>
                    <TextBox  Text="{Binding RepeatPassword}" x:Name="Pass2"/>
                    <Button Content="Зарегистрировать" Margin="5" Click="NewDoctor"/>
                </StackPanel>
            </Border>
            <Border  BorderBrush="Black" BorderThickness="1" Margin="5" Width="180" Height="110">
                <StackPanel Margin="5" x:Name="D_SignInForm">
                    <TextBlock Text="Идентификатор:"/>
                    <TextBox x:Name="D_SignId"/>
                    <TextBlock Text="Пароль:"/>
                    <PasswordBox x:Name="D_SignPass"/>
                    <Button Margin="5" Content="Войти" Click="D_SignIn"></Button>
                </StackPanel>
            </Border>
            <Border BorderBrush="Black" BorderThickness="1" Height="215" Width="180" Margin="5">
                <WrapPanel Margin="5" Orientation="Vertical" x:Name="D_current">
                    <TextBlock Padding="1" Text="Идентификатор:"/>
                    <TextBlock Padding="1" Text="{Binding Id}"/>
                    <TextBlock Padding="1" Text="Фамилия:"/>
                    <TextBlock Padding="1" Text="{Binding LastName}"/>
                    <TextBlock Padding="1" Text="Имя:"/>
                    <TextBlock Padding="1" Text="{Binding Name}"/>
                    <TextBlock Padding="1" Text="Отчество:"/>
                    <TextBlock Padding="1" Text="{Binding MiddleName}"/>
                    <TextBlock Padding="1" Text="Специальность:"/>
                    <TextBlock Padding="1" Text="{Binding Specialisation}"/>
                </WrapPanel>
            </Border>
           
        </WrapPanel>
        <Border HorizontalAlignment="Right" VerticalAlignment="Top" BorderBrush="Black" BorderThickness="1" Margin="5" Grid.Column="1" Height="590">
        <WrapPanel Orientation="Vertical"  x:Name="P_current">
            <StackPanel Width="240"  Orientation="Horizontal" x:Name="P_Search">
                <TextBox Width="200" Margin="5" Text="{Binding Id}"/>
                    <Button Width="30" Click="Search">🏓</Button>
            </StackPanel>
            <WrapPanel Orientation="Vertical">
                <TextBlock Padding="1" Text="Идентификатор:"/>
                <TextBlock Padding="1" Text="{Binding Id}"/>
            </WrapPanel>
            <WrapPanel Orientation="Vertical">
                <TextBlock Padding="1" Text="Фамилия:"/>
                <TextBlock Padding="1" Text="{Binding LastName}"/>
            </WrapPanel>
            <WrapPanel Orientation="Vertical">
                <TextBlock Padding="1" Text="Имя:"/>
                <TextBlock Padding="1" Text="{Binding Name}"/>
            </WrapPanel>
            <WrapPanel Orientation="Vertical">
                <TextBlock Padding="1" Text="Отчество:"/>
                <TextBlock Padding="1" Text="{Binding MiddleName}"/>
            </WrapPanel>
            <WrapPanel Orientation="Vertical">
                <TextBlock Padding="1" Text="День рождения:"/>
                <TextBlock Padding="1" Text="{Binding Birthday}"/>
            </WrapPanel>
            <WrapPanel Orientation="Vertical">
                <TextBlock Padding="1" Text="Последнее обращение:"/>
                <DatePicker Padding="1" Width="240" SelectedDate="{Binding LastVisit}"/>
            </WrapPanel>
            <WrapPanel Orientation="Vertical">
                <TextBlock Padding="1" Text="Последний доктор:"/>
                <TextBlock Padding="1" Text="{Binding LastDoctorName}"/>
            </WrapPanel>
            <WrapPanel Orientation="Vertical">
                <TextBlock Padding="1" Text="Диагноз:"/>
                <TextBox Width="240" Padding="1" Text="{Binding Diagnosis}"/>
            </WrapPanel>
            <WrapPanel Orientation="Vertical">
                <TextBlock Padding="1" Text="Рекомендации:"/>
                <TextBox Height="50" Width="240" Padding="1" Text="{Binding Recomendation}"/>
            </WrapPanel>
            <Button Margin="5" Content="Начать прием пациента" Click="StartNewVisit"/>
            <Button Margin="5" Content="Сохранить" Click="SaveNewData"/>
            <Button Margin="5" Content="Завершить прием пациента" Click="ResetPatient"/>
           
        </WrapPanel>
        </Border>
        <DockPanel HorizontalAlignment="Left" VerticalAlignment="Top" Width="330" Height="600"  Grid.Column="2" Margin="5" Grid.RowSpan="2"  >
            <Border BorderBrush="Black" BorderThickness="1" Height="295" Margin="5"  DockPanel.Dock="Top">
                <StackPanel Margin="5" x:Name="P_RegisterForm">
                    <TextBlock Text="Фамилия:"/>
                    <TextBox Text="{Binding LastName}"/>
                    <TextBlock Text="Имя:"/>
                    <TextBox  Text="{Binding Name}"/>
                    <TextBlock Text="Отчество:"/>
                    <TextBox  Text="{Binding MiddleName}"/>
                    <TextBlock Text="Дата рождения:"/>
                    <DatePicker SelectedDate="{Binding Birthday}" x:Name="P_BD"></DatePicker>
                    <Button Content="Зарегистрировать" Margin="5" Click="NewPatient"/>
                </StackPanel>
            </Border>
            <Border BorderBrush="Black" BorderThickness="1" Height="275" Margin="5" DockPanel.Dock="Bottom">
                <StackPanel Margin="5" x:Name="P_Change">
                    <TextBlock Text="Фамилия:"/>
                    <TextBox Text="{Binding LastName}"/>
                    <TextBlock Text="Имя:"/>
                    <TextBox  Text="{Binding Name}"/>
                    <TextBlock Text="Отчество:"/>
                    <TextBox  Text="{Binding MiddleName}"/>
                    <TextBlock Text="Дата рождения:"/>
                    <DatePicker  SelectedDate="{Binding Birthday}" x:Name="P_BD2"></DatePicker>
                    <Button Content="Изменить" Margin="5" Click="ChangePatient"/>
                    <Button Content="Сбросить" Margin="5" Click="ResetChanges"/>

                </StackPanel>
            </Border>
        </DockPanel>
        <Border BorderBrush="Black" HorizontalAlignment="Left" Width="775" Margin="5" BorderThickness="1" Grid.Row="1" Grid.ColumnSpan="3" >
            <WrapPanel HorizontalAlignment="Left" VerticalAlignment="Center" x:Name="InfoPanel">
                <TextBlock FontSize="14" Text="Количество пользователей (Докторов) и пациентов: " x:Name="Count"></TextBlock>
                <TextBlock FontSize="14" Width="20" Text="{Binding All_count}"/>
                <TextBlock FontSize="14" Text=" | Количество пациентов: "></TextBlock>
                <TextBlock FontSize="14" Width="20" Text="{Binding P_count}"/>
                <TextBlock FontSize="14" Text=" | Количество Докторов: "></TextBlock>
                <TextBlock FontSize="14" Width="20" Text="{Binding D_count}"/>

            </WrapPanel>
        </Border>
    </Grid>-->
    <Grid>
        <Frame Name="MainFrame" NavigationUIVisibility="Hidden"/>
    </Grid>
</Window>
