<Page x:Class="_7_TRPO.Pages.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:_7_TRPO.Pages"
      mc:Ignorable="d" 
      
      Title="Войти">

    <StackPanel>
        <TextBlock Text="Идентификатор:"/>
        <TextBox x:Name="D_SignId"/>
        <TextBlock Text="Пароль:"/>
        <PasswordBox x:Name="D_SignPass"/>
        <Button Style="{StaticResource Standart}" Content="Войти" Click="Login_Click"></Button>
        <Button Style="{StaticResource Standart}" Content="Зарегистрироваться" Click="Register_Click"></Button>

    </StackPanel>
</Page>
